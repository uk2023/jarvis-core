import React,{useState,useEffect}from'react';
import{Moon,CheckCircle2,Clock,RefreshCw,FlaskConical,ShieldAlert,Sparkles}from'lucide-react';
import{OvernightLogEntry,AppTheme}from'../types';
import{api}from'../api/client';
import{TabHeader}from'./TabHeader';

interface OvernightLearningCardProps{theme?:AppTheme}
export const OvernightLearningCard:React.FC<OvernightLearningCardProps>=({theme='dark'})=>{
const isDark=theme==='dark';const[logs,setLogs]=useState<OvernightLogEntry[]>([]);const[isLoading,setIsLoading]=useState(false);
const loadData=async()=>{setIsLoading(true);try{const data=await api.getOvernightLog();setLogs(data)}finally{setIsLoading(false)}};useEffect(()=>{loadData()},[]);
const headerControls=<><span className="trace-tab-control">SANDBOX-TESTED</span><button type="button" onClick={loadData} disabled={isLoading} className="trace-action-btn trace-refresh" title="Refresh report" aria-label="Refresh report"><RefreshCw size={14} className={isLoading?'animate-spin':''}/></button></>;
return <section id="card-overnight-learning" className={`trace-root ${isDark?'trace-dark':''} mx-auto w-full max-w-xl overflow-visible text-xs`}>
<TabHeader icon={Moon} category="JARVIS / AUTONOMY" title="OVERNIGHT / IDLE LEARNING" subtitle="Sandbox-tested findings • Continuous adaptation telemetry" controls={headerControls}/>
<div className="trace-flow px-3 pb-3 sm:px-3.5"><div className="trace-flow-label text-[10px] font-semibold tracking-wider uppercase"><Sparkles size={12}/>IDLE LEARNING FINDINGS <span>• REAL SANDBOX-TESTED EVIDENCE</span></div>
<p className="mb-2.5 font-mono text-[10px] leading-normal text-slate-500 dark:text-slate-400">Audits vocabulary and syntactic patterns explored during battery idle loops. Only candidates passing synthetic sandbox tests are recorded.</p>
<div className="grid grid-cols-1 gap-2 sm:grid-cols-2">{logs.map((entry,idx)=>{const dateStr=new Date(entry.timestamp).toLocaleString();const hasFindings=entry.findings.length>0;return <article key={idx} className="rounded-2xl border border-slate-200/60 bg-slate-50/50 p-2.5 shadow-sm dark:border-slate-700/70 dark:bg-slate-900/45"><div className="flex flex-col gap-1.5 border-b border-slate-200/80 pb-2 dark:border-slate-700/60"><div className="flex flex-wrap items-center justify-between gap-1.5"><div className="flex min-w-0 items-center gap-1.5"><Clock className="h-3 w-3 shrink-0 text-slate-400"/><span className="font-mono text-[10px] leading-normal text-slate-500 dark:text-slate-400">{dateStr}</span></div><span className="max-w-full truncate rounded-full border border-brand-200 bg-brand-50 px-2 py-0.5 font-mono text-[9px] font-bold uppercase tracking-wide text-brand-700 dark:border-brand-500/20 dark:bg-brand-500/10 dark:text-brand-300">{entry.reviewed_target}</span></div><div className="flex items-center justify-between gap-2 font-mono text-[9px] uppercase tracking-wide"><span className="text-slate-400">Evidence Threshold</span>{entry.evidence_threshold_met?<span className="inline-flex items-center gap-1 rounded-full border border-emerald-200 bg-emerald-50 px-2 py-0.5 font-bold text-emerald-700 dark:border-emerald-500/20 dark:bg-emerald-500/10 dark:text-emerald-300"><CheckCircle2 className="h-3 w-3"/>MET</span>:<span className="inline-flex items-center gap-1 rounded-full border border-amber-200 bg-amber-50 px-2 py-0.5 font-bold text-amber-700 dark:border-amber-500/20 dark:bg-amber-500/10 dark:text-amber-300"><ShieldAlert className="h-3 w-3"/>UNMET</span>}</div></div><div className="mt-2 rounded-xl border border-slate-200/70 bg-white/70 p-2.5 font-mono text-xs leading-normal text-slate-700 dark:border-slate-700/70 dark:bg-slate-950/30 dark:text-slate-200">{entry.summary}</div>{hasFindings?<div className="mt-2 grid grid-cols-1 gap-1.5">{entry.findings.map(f=><div key={f.id} className="rounded-xl border border-slate-200/70 bg-white p-2.5 shadow-sm dark:border-slate-700/70 dark:bg-slate-900/70"><div className="flex flex-col gap-1.5 sm:flex-row sm:items-start sm:justify-between"><div className="min-w-0"><div className="break-words font-mono text-xs font-bold leading-normal text-slate-800 dark:text-slate-100">{f.pattern_or_vocab}</div><div className="mt-0.5 font-mono text-[9px] leading-normal text-slate-400">{f.pass_count}P / {f.fail_count}F verified runs</div></div><span className={`inline-flex w-fit shrink-0 items-center gap-1 rounded-full border px-2 py-0.5 font-mono text-[9px] font-bold uppercase tracking-wide ${f.test_passed?'border-emerald-200 bg-emerald-50 text-emerald-700 dark:border-emerald-500/20 dark:bg-emerald-500/10 dark:text-emerald-300':'border-rose-200 bg-rose-50 text-rose-700 dark:border-rose-500/20 dark:bg-rose-500/10 dark:text-rose-300'}`}><FlaskConical className="h-3 w-3"/>{f.test_passed?'SANDBOX PASS':'SANDBOX FAIL'}</span></div><div className="mt-1.5 rounded-lg bg-slate-50/80 px-2 py-1.5 font-mono text-[10px] leading-normal text-slate-600 dark:bg-slate-950/40 dark:text-slate-300"><span className="font-bold text-brand-600 dark:text-brand-300">Evidence:</span> {f.evidence}</div></div>)}</div>:<div className="mt-2 rounded-xl border border-amber-200 bg-amber-50/70 p-2.5 text-center dark:border-amber-500/20 dark:bg-amber-500/5"><div className="flex items-center justify-center gap-1.5 font-mono text-[10px] font-bold text-amber-700 dark:text-amber-300"><ShieldAlert className="h-3 w-3"/>Honest Empty State: Nothing met evidence threshold</div><p className="mt-1 font-mono text-[9px] leading-normal text-amber-700/70 dark:text-amber-300/70">Curiosity loop requires &gt;= 0.85 confidence and 3 verified test runs before registering new knowledge.</p></div>}</article>})}</div><div className="mt-3 text-center font-mono text-[9px] font-bold tracking-[0.12em] text-slate-400">• TRACE END · SANDBOX AUDIT COMPLETE</div></div></section>;
};