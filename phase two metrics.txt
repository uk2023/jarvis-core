JARVIS-NEURAL — PHASE 2 METRICS BASELINE
=========================================
Branch: jarvis-neural

PURPOSE
-------
Phase 2 establishes a repeatable measurement layer before any fruit-fly /
connectome-inspired neural experiment is introduced.

IMPLEMENTED
-----------
1. Added tools/phase2_metrics.py.
2. Production cognition code is not modified by the harness.
3. Existing deterministic E2E scenario suite can be measured as a functional
   regression score (PASS / total scenarios) plus suite wall-clock time.
4. Optional JSONL trace aggregation measures, when the runtime trace exposes
   compatible timestamps/events:
   - latency samples (mean/median/min/max)
   - LLM call count
   - prompt/completion/total tokens
   - event counts
5. Reports can be emitted as JSON for repeatable baseline snapshots.

ACTUAL BASELINE RUN — 2026-09-22
--------------------------------
Command:
  python3 tools/phase2_metrics.py

Environment:
  Python 3.14.6
  Linux / Android PRoot runtime

Offline deterministic regression:
  Passed: 23
  Failed: 0
  Total: 23
  Functional regression pass rate: 100.0%
  Suite elapsed time: 8.7219 seconds
  Timeout: false
  Return code: 0

INTERPRETATION
--------------
The 100.0% value is the pass rate of the existing deterministic regression
scenarios. It is NOT a claim that JARVIS has 100% general accuracy, factual
accuracy, or task success.

LIVE METRICS
------------
The current run did not include a JSONL runtime trace, so these remain
unmeasured rather than being guessed:
- real response latency
- context carryover across live turns
- interruption/recovery
- live action success/failure
- state stability
- real LLM call/token totals

The harness already supports importing compatible JSONL runtime traces:
  python3 tools/phase2_metrics.py --trace <runtime-trace.jsonl> --json-out phase2_baseline.json

METRIC DEFINITIONS
------------------
Functional accuracy = deterministic scenario checks passed / total checks.
This is a regression metric, not a claim about general intelligence or factual
accuracy.

Response latency = elapsed time between compatible start/end timestamps in a
runtime trace. If the trace does not expose a valid pair, the metric remains
unknown rather than being estimated.

LLM calls/tokens = values explicitly present in the runtime trace. The harness
does not infer tokens from prompt length.

Context carryover, interruption handling, action success/failure and state
stability require scenario-specific evidence. They must not be marked PASS
merely because a component exists. Live Android/PRoot behavior must be measured
on-device.

PHASE 2 STATUS
--------------
CODE/HARNESS: COMPLETE
OFFLINE REGRESSION BASELINE: COMPLETE — 23/23 (100.0%)
LIVE ANDROID/PRoot BASELINE: PENDING ON-DEVICE TRACE

NEXT GATE
---------
Before Phase 3, capture a representative live runtime trace from the current
JARVIS and feed it to the harness. Do not add neural code before that baseline
is recorded.

IMPORTANT
---------
No fruit-fly connectome code, simulator, or neural controller has been added.
No production Jarvis behavior is changed by this Phase-2 harness.